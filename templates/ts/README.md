# @hahawurld/<%= name %>

<%= description %>

## Usage

`npm i --save-dev @hahawurld/<%= name %>`

## Contributing

Submit a PR!

## Release

The release GitHub action will publish a new release triggered on push of a new release tag using `npm version`.
